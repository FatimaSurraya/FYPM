@model List<FYPM.Models.ProjectDetail>
@{
    ViewBag.Title = "StudentProjectGrid";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2 style="margin-left:10px">List of Projects offered</h2>

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-content">
                    <div class="wrapper wrapper-content">
                        <div class="row">
                            <table class="table  table-hover table-responsive table-striped" id="mttable">
                                <thead>
                                    <tr>
                                        <th>Title</th>
                                        <th>Project Description</th>
                                        <th>Total Marks</th>
                                        <th>Students Allowed</th>
                                        <th>Project Request</th>
                                    </tr>
                                </thead>
                                <tbody id="table_body" class="scrollable">
                                    @foreach (var projectDetail in Model)
                                    {
                                        <tr>
                                            <td>@projectDetail.Title</td>
                                            <td><iframe src="@projectDetail.ProjectDocuments" width="100%" height="500px"></iframe></td>
                                            <td>100</td>
                                            <td>@projectDetail.StudentsAllowed</td>
                                            <td>
                                                <a data-project-id="@projectDetail.ProjectId" class="btn btn-success register-button">Project Request</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<script>
    $(document).ready(function () {
        $(".register-button").click(function () {
            var projectId = $(this).data("project-id");

            $.ajax({
                type: "POST",
                url: "/Project/RegisterProject",
                data: { projectId: projectId },
                success: function (response) {
                    if (response == 1) {
                        alert("Project request sent to supervisor!");
                    } else {
                        alert("Failed to send project request. " + response.message);
                    }
                },
                error: function () {
                    alert("An error occurred while registering the project.");
                }
            });
        });
    });
</script>

